---
trigger: 
  - master

pool: Pipeline

stages:
  - stage: 'imagebuild_1'
    jobs:
      - job: 'createcontainer'
        steps:
          - task: Docker@2
            inputs:
              containerRegistry: Python
              repository: src
              command: 'buildAndPush'
              Dockerfile: '**/Dockerfile'
              buildContext: '**'
              tags: '$(Build.BuildId)'
